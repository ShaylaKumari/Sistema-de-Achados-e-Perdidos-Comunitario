PORT=3000 #ou qualquer outra
DB_HOST=localhost
DB_USER=usuario_do_postgres
DB_PASS=senha_do_postgres
DB_NAME=achdos_e_perdidos_db

DATABASE_URL="postgresql://${DB_USER}:${DB_PASS}@${DB_HOST}:5432/${DB_NAME}"
